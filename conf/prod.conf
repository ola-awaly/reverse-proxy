# http {
# log_format main '$remote_addr - $remote_user [$time_local] "$request" '
#                    '$status $body_bytes_sent "$http_referer" '
#                    '"$http_user_agent" "$http_x_forwarded_for"';
    
#     access_log /var/log/nginx/access.log main;
#     error_page 404 /404.html;
server {
    
    server_name mastermind.olatech.ovh 
    location / {
        proxy_pass http://mastermindclient; # Dirige tout vers le service React
        # proxy_http_version 1.1;
        # proxy_set_header Upgrade $http_upgrade;
        # proxy_set_header Connection 'upgrade';
        # proxy_set_header Host $host;
        # proxy_cache_bypass $http_upgrade;
        #try_files $uri /; # Laisse React gérer les chemins côté client
    
    }
    location /api {
    #resolver 127.0.0.11 valid=10s;
   # set $upstream_mastermindserver mastermindserver;
    proxy_pass http://mastermindserver:8080;
    
    }
#     location @proxymastermindclient {
#         proxy_pass http://$upstream_mastermindclient;
#          }
 }

server {
    server_name cvgenerator.olatech.ovh
    location / {
    
    proxy_pass http://client;
   
   
    }
    location /api {
       
        proxy_pass http://server:8080;
    }
     
}


server {
    server_name olatech.ovh
    location / {
    
    proxy_pass http://monportfolio;
   
   
    }
   
     
}
